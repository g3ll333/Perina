// scene2.js
function preload(s) {
    // Carica le risorse necessarie per la scena 2
}

function create(s) {
    // Configura gli elementi della scena 2
    console.log("scena 2 creata!");
}

function update(s) {
    // Gestisci il comportamento della scena 2
}

function destroy(s) {
    // Pulisci gli elementi della scena 2
}

PP.scenes.add("scene2", preload, create, update, destroy);
